{
  "name": "charto-store",
  "version": "0.0.1",
  "description": "Geometry data storage",
  "main": "index.js",
  "scripts": {
    "autogypi": "autogypi",
    "node-gyp": "node-gyp",
    "emcc-path": "emcc-path",
    "copyasm": "copyasm",
    "ndts": "ndts",

    "test": "rm -rf build && node-gyp configure build && node test.js",
    "test-asm": "rm -rf build && node-gyp configure build --asmjs=1 && node test.js"
  },
  "author": "Juha Järvi",
  "license": "MIT",
  "dependencies": {
    "autogypi": "~0.2.2",
    "nbind": "~0.3.1",
    "node-gyp": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^1.8.10"
  }
}
